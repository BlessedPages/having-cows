<p id="notice" class="anchor"><%= notice %></p>

<h1 class="funky-txt anchor">All Products</h1>

<div class="container">
  <div class="row">
  <% cache(cache_key_for_products) do %>
  	<%= render @products %>
  <% end %>
  </div> <!-- end row -->
</div> <!-- end container -->

<br>
<% if user_signed_in? && current_user.admin? %>
	<%= link_to 'New Product', new_product_path %>
<% end %>